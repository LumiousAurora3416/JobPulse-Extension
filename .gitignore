# 环境变量 / 敏感配置
.env
.env.local
.env.*.local
.codex/

# 编辑器 / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# 依赖（本项目无 node_modules，留作规范）
node_modules/
dist/

# 构建产物
*.zip
*.crx
*.pem

# Python __pycache__
__pycache__/
*.pyc

# Agent 本地配置（含 API Key）
agent/config.local.py

# Runtime data
agent/message_store.json
agent/conversation_store.json
agent/user_profile.json
agent/resume.txt

# R
.Rhistory

# 本地素材库（面试素材 / 思考过程 / 复盘，绝不上传 GitHub）
# 所有不想外泄的文件统一放 private/ 目录，一行挡住整个文件夹
private/

# 简历（Word 版，与 resume.txt 一样是本地素材，不上传）
agent/resume.docx

# 岗位匹配度评测（标注集含简历摘录，隐私；缓存/模板为生成产物）
agent/match_eval/eval_set.json
agent/match_eval/eval_output.json
agent/match_eval/eval_set.template.json
